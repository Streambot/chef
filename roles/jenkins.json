{
  "name": "jenkins",
  "description": "Jenkins server role",
  "json_class": "Chef::Role",
  "description": "This the Jenkins role - a Jenkins CI server.",
  "chef_type": "role",
  "run_list": [
    "recipe[jenkins::server]",
    "recipe[setup-jenkins]"
  ],
  "env_run_lists":{
    "_default": [
    ],
    "production": [
      "recipe[jenkins::server]",
      "recipe[setup-jenkins]"
    ]
  }
}